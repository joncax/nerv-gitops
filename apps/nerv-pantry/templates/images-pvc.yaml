apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.images.pvcName }}
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.images.storage }}
