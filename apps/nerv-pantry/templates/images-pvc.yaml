apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: images-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.images.storage }}
