apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: magic-tavern-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/joncax/nerv-gitops.git
    targetRevision: main
    path: apps/magic-tavern
    helm:
      valueFiles:
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: magic-tavern
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
